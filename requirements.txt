dash>=4.0
plotly>=6.0
pandas>=2.2
numpy>=1.26
yfinance>=0.2.50
requests>=2.31
beautifulsoup4>=4.12
lxml>=5.0
anthropic>=1.0
python-dotenv>=1.0

# Deployment (Render runs `gunicorn app:server`; not needed for `python app.py` locally)
gunicorn>=21.2

# Testing (`pytest` from the project root)
pytest>=8.0
